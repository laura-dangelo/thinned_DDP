% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampler_DP.R
\name{sampler_DP}
\alias{sampler_DP}
\title{Gibbs sampler for the DP Gaussian mixture model}
\usage{
sampler_DP(
  nrep,
  burnin,
  thinning_factor = 2,
  y,
  trunc = 50,
  m0 = 0,
  tau0 = 0.1,
  gamma0 = 3,
  lambda0 = 2,
  alpha = 1,
  mu_start,
  sigma2_start,
  cl_start,
  progressbar = TRUE
)
}
\arguments{
\item{nrep}{Integer. Total number of MCMC iterations.}

\item{burnin}{Integer. Number of initial iterations discarded as burn-in.}

\item{thinning_factor}{Integer. Thinning interval used to store posterior
samples. For example, `thinning_factor = 2` retains every second draw.}

\item{y}{Numeric vector of observations.}

\item{trunc}{Integer. Truncation level of the stick-breaking representation,
corresponding to the maximum number of mixture components.}

\item{m0}{Prior mean hyperparameter for component means.}

\item{tau0}{Prior precision parameter for component means. The prior is
\eqn{\mu_j \mid \sigma_j^2 \sim N(m_0, \sigma_j^2 / \tau_0)}.}

\item{gamma0}{Shape hyperparameter of the inverse-Gamma prior on component
variances.}

\item{lambda0}{Rate hyperparameter of the inverse-Gamma prior on component
variances. The prior is
\eqn{1/\sigma_j^2 \sim \mathrm{Gamma}(\gamma_0, \lambda_0)}.}

\item{alpha}{Positive scalar. Concentration parameter of the Dirichlet
process controlling the stick-breaking weights.}

\item{mu_start}{Numeric vector of length `trunc` containing initial values
for the component means.}

\item{sigma2_start}{Numeric vector of length `trunc` containing initial
values for the component variances.}

\item{cl_start}{Numeric or integer vector of initial cluster allocations.
Values should be in `{0, ..., trunc - 1}`.}

\item{progressbar}{Logical. If `TRUE`, displays a progress bar during MCMC
sampling.}
}
\value{
A list containing:
\describe{
  \item{mu}{Matrix of posterior samples for component means. Rows correspond
  to retained MCMC iterations and columns to mixture components.}
  \item{sigma2}{Matrix of posterior samples for component variances.}
  \item{cl}{Matrix of posterior samples for cluster allocations.}
  \item{pi}{Matrix of posterior samples for the mixture weights.}
  \item{y}{Observed data.}
  \item{nrep}{Total number of MCMC iterations.}
  \item{burnin}{Number of burn-in iterations.}
  \item{trunc}{Truncation level of the stick-breaking representation.}
  \item{hyperpar}{Vector containing the prior hyperparameters
  \code{(m0, tau0, gamma0, lambda0)}.}
  \item{alpha}{Dirichlet process concentration parameter.}
  \item{mu_start}{Initial values for component means.}
  \item{sigma2_start}{Initial values for component variances.}
  \item{cl_start}{Initial cluster allocations.}
  \item{time}{Elapsed computation time.}
}
}
\description{
Gibbs sampler for the DP Gaussian mixture model
}
